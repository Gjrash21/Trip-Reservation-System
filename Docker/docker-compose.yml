version: '3.8'

services:
  web:
    build: .
    container_name: trip_app
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - sqlite_data:/app/data
    environment:
      - FLASK_ENV=development

volumes:
  sqlite_data:
